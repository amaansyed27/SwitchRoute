# GitHub Copilot

Canonical repository guidance lives in `/AGENTS.md`. Read and follow it before making changes.

When editing `apps/web`, `services/gateway`, or `supabase`, also read the nearest scoped `AGENTS.md`. There are no Copilot-specific overrides beyond those repository instructions.
