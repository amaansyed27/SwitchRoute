# OpenRouter setup

Create an OpenRouter API key and choose **OpenRouter** on the Providers page. SwitchRoute validates the key, discovers models, and uses the returned pricing metadata to distinguish clearly free models from paid models for basic Free First routing.

OpenRouter's account/provider behavior can change independently of SwitchRoute; re-run **Test** to refresh connection health and model metadata.
