"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { Drawer } from "@/components/ui/drawer";
import { Badge, EmptyState, LoadingBlock, Retry, StatusDot } from "@/components/ui/feedback";
import { Input } from "@/components/ui/form";
import { Icon } from "@/components/ui/icon";
import { Card, PageHeader } from "@/components/ui/surface";
import type { ActivityRecord } from "@/features/shared/types";
import { manageFetch } from "@/lib/gateway/manage";

function costLabel(value?: number | null) { return value == null ? "—" : `$${(value / 1_000_000).toFixed(value < 10_000 ? 6 : 4)}`; }

export function ActivityClient() {
  const [activity, setActivity] = useState<ActivityRecord[] | null>(null); const [selected, setSelected] = useState<ActivityRecord | null>(null); const [query, setQuery] = useState(""); const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { try { const result = await manageFetch<ActivityRecord[]>("activity?limit=100"); setActivity(result); setError(null); } catch (err) { setError(err instanceof Error ? err.message : "Activity could not be loaded."); } }, []);
  useEffect(() => { let cancelled = false; manageFetch<ActivityRecord[]>("activity?limit=100").then((result) => { if (cancelled) return; setActivity(result); setError(null); }).catch((err: unknown) => { if (cancelled) return; setError(err instanceof Error ? err.message : "Activity could not be loaded."); }); return () => { cancelled = true; }; }, []);
  const rows = useMemo(() => (activity ?? []).filter((item) => `${item.route_name} ${item.provider_kind ?? ""} ${item.model_id ?? ""} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [activity, query]);
  return <div><PageHeader title="Activity" eyebrow="Metadata only" description="Routing diagnostics without prompt, completion, tool, system-prompt, or upload content." action={<div className="relative w-56"><Icon name="search" className="pointer-events-none absolute left-2.5 top-2.5 size-4 text-[var(--muted-foreground)]"/><Input className="pl-8" aria-label="Filter activity" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Filter requests"/></div>}/>{error && <Retry message={error} onRetry={() => void load()}/>} {activity === null && !error ? <LoadingBlock label="Loading activity"/> : activity && !activity.length ? <EmptyState title="No requests yet" description="Successful and failed routed requests will appear here as sanitized operational metadata."/> : activity && <Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[1040px] text-left"><thead className="bg-[var(--surface-muted)] text-[11px] uppercase tracking-[.06em] text-[var(--muted-foreground)]"><tr><th className="px-4 py-2.5 font-medium">Time</th><th className="px-4 py-2.5 font-medium">Provider / model</th><th className="px-4 py-2.5 font-medium">Waterfall</th><th className="px-4 py-2.5 font-medium">Latency</th><th className="px-4 py-2.5 font-medium">Tokens</th><th className="px-4 py-2.5 font-medium">Cost</th><th className="px-4 py-2.5 font-medium">Fallbacks</th><th className="px-4 py-2.5 font-medium">Status</th><th/></tr></thead><tbody className="divide-y divide-[var(--border)]">{rows.map((item) => <tr key={`${item.request_id}-${item.created_at}-${item.provider_kind ?? "none"}`} className="hover:bg-[var(--surface-hover)]"><td className="px-4 py-3 text-xs text-[var(--muted-foreground)]">{new Date(item.created_at).toLocaleString()}</td><td className="px-4 py-3"><strong className="block text-xs">{item.provider_kind ?? "—"}</strong><code className="mt-0.5 block max-w-56 truncate font-mono text-[11px] text-[var(--muted-foreground)]">{item.model_id ?? "—"}</code></td><td className="px-4 py-3 text-xs">{item.route_name}</td><td className="px-4 py-3 text-xs">{item.latency_ms} ms{item.ttft_ms != null && <span className="block text-[10px] text-[var(--muted-foreground)]">TTFT {item.ttft_ms} ms</span>}</td><td className="px-4 py-3 text-xs text-[var(--muted-foreground)]">{item.input_tokens ?? "?"} → {item.output_tokens ?? "?"}</td><td className="px-4 py-3 font-mono text-[11px]">{costLabel(item.estimated_cost_microusd)}</td><td className="px-4 py-3 text-xs">{item.fallback_count || "—"}</td><td className="px-4 py-3"><span className="flex items-center gap-2 text-xs capitalize"><StatusDot status={item.status}/>{item.status}</span></td><td className="px-4 py-3 text-right"><Button variant="ghost" size="sm" onClick={() => setSelected(item)}>Details</Button></td></tr>)}</tbody></table></div></Card>}{selected && <Drawer title="Request details" description="Sanitized routing metadata only." onClose={() => setSelected(null)}><dl className="space-y-2">{[["Request ID", selected.request_id], ["Status", selected.status], ["Waterfall", selected.route_name], ["Provider", selected.provider_kind ?? "Unavailable"], ["Model", selected.model_id ?? "Unavailable"], ["Latency", `${selected.latency_ms} ms`], ...(selected.ttft_ms != null ? [["TTFT", `${selected.ttft_ms} ms`]] : []), ["Tokens", `${selected.input_tokens ?? "?"} in / ${selected.output_tokens ?? "?"} out`], ["Estimated cost", costLabel(selected.estimated_cost_microusd)], ["Fallbacks", String(selected.fallback_count)], ...(selected.error_category ? [["Error category", selected.error_category]] : []), ...(selected.routing_decision?.effective_strategy ? [["Strategy", selected.routing_decision.effective_strategy]] : []), ...(selected.routing_decision?.selected?.reason ? [["Why selected", selected.routing_decision.selected.reason]] : []), ...(selected.routing_decision?.quota?.source ? [["Quota confidence", `${selected.routing_decision.quota.source}${selected.routing_decision.quota.confidence != null ? ` · ${Math.round(selected.routing_decision.quota.confidence * 100)}%` : ""}`]] : [])].map(([label,value]) => <div key={label} className="grid grid-cols-[120px_minmax(0,1fr)] gap-4 border-b border-[var(--border)] py-3"><dt className="text-xs text-[var(--muted-foreground)]">{label}</dt><dd className="break-all text-xs font-medium">{value}</dd></div>)}</dl>{selected.routing_decision?.path?.length ? <div className="mt-5"><p className="mb-2 text-xs font-medium">Routing path</p><ol className="space-y-2">{selected.routing_decision.path.map((step, index) => <li key={`${step.provider}-${step.model}-${index}`} className="rounded-lg border border-[var(--border)] px-3 py-2 text-xs"><span className="font-mono text-[var(--muted-foreground)]">{String(index + 1).padStart(2,"0")}</span> <strong className="ml-2">{step.provider}</strong> · {step.model}<span className="ml-2 text-[var(--muted-foreground)]">{step.outcome}</span></li>)}</ol></div> : null}<div className="mt-5"><Badge>Content intentionally unavailable</Badge></div></Drawer>}</div>;
}
